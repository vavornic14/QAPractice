{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "tests/regression/ holds three tests that are SUPPOSED to fail - they encode unfixed bugs (REG-01/02/03). This denies agent edits to them so a test healer cannot weaken an assertion until it passes, which would erase the only record of three critical defects. Lift it only to add a new regression test, then restore. See tests/regression/README.md and CLAUDE.md.",
  "permissions": {
    "deny": [
      "Edit(./tests/regression/**)",
      "Write(./tests/regression/**)"
    ]
  }
}
